import { Helmet } from "react-helmet-async";

const SignIn = () => {
    return (
        <div>
            <Helmet>
                <title>SkillBoost Hub || sign in</title>
            </Helmet>
        </div>
    );
};

export default SignIn;